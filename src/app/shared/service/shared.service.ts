import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})

/**
 I know this is not a recommended to create unused service,
  Its just for illustrate purpose, we can utilize this as a global service within application. 
 */
export class SharedService {

  constructor() { }
}
