/**
 This document is consider only when the application is in development mode. The variable 'isProduction' helps to understand 
 inside the component / services. It's a easily acccessible inside the solution. 
 */

export const environment = {
    isProduction: false,
    liveApiUrl: 'https://my-json-server.typicode.com/fazalrahim/json_api_iq'
};
